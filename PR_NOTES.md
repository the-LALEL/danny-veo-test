# Doorbell capture-core review notes

This branch isolates Milestone A from the prior experimental work.

It intentionally excludes ChatGPT delivery, artifact encryption, databases, queues, and persistent hosting until one real fresh Nest frame is proven.

See `DOORBELL_CAPTURE.md` for usage and `CAPTURE_STATUS.md` for the verified state and current blocker.
